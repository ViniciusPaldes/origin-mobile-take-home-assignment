import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  actionsContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 8,
  },
  action: {
    paddingHorizontal: 8,
  },
});
